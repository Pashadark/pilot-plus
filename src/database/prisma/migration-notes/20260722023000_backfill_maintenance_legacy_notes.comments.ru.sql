-- Переносим legacy-описания технического обслуживания в новое видимое пользователю поле notes.
-- Этот follow-up намеренно не изменяет уже применённую миграцию 20260721230000.
-- OVERDUE теперь является вычисляемым статусом чтения: PLANNED плюс прошедший scheduledAt.
-- Проверяем типизированный результат. Неизвестные текстовые значения должен остановить preflight
-- до преобразования enum; после предыдущей миграции PostgreSQL допускает только значения enum.
